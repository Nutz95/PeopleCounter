# Profil : CPU Fallback (Économie énergie ou pas de matériel compatible)
# Tout tourne sur le processeur central.
export YOLO_BACKEND=torch
export YOLO_DEVICE=cpu
export YOLO_SEG=1
export YOLO_MODEL=models/pt/yolo11s-seg.pt
export LWCC_BACKEND=torch
